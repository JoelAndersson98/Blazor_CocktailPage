<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <a class="navbar-brand" href="#">
            <i class="fas fa-cocktail d-inline-block align-text-top"></i>
            <span>Blazor Cocktails</span>
        </a>
        <button class="hamburger hamburger--collapse navbar-toggler @(IsActive ? "is-active" : "")" type="button" data-bs-toggle="collapse" 
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" 
                aria-label="Toggle navigation" @onclick="ToggleHamburgerMenu">
            <span class="hamburger-box">
                <span class="hamburger-inner"></span>
            </span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <NavLink class="nav-link" href="">
                        Drinks
                    </NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="Ingredients">
                        Ingredients
                    </NavLink>
                </li>
            </ul>
        </div>
    </div>
</nav>

@code {
    private bool IsActive { get; set; } = false;

    private void ToggleHamburgerMenu()
    {
        IsActive = !IsActive;
    }
}
